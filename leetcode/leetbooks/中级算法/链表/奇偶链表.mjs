export { oddEvenList };
import { ListNode } from "../../../classes/ListNode.mjs";
/**
 * @param {ListNode} head
 * @returns {ListNode}
 */
const oddEvenList = function(head) {
    const headLog = head;
    let count = 0;
    let p = head.next;
    while (p) {
        //
    }
    return headLog;
}
